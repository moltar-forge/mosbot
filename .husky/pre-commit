#!/bin/sh

# Run lint-staged (Prettier formatting on staged files)
npx lint-staged

# Run gitleaks if installed (secret scanning on staged files)
if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact --no-banner
fi
