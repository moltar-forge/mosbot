# workspace-server

Workspace sidecar service used by MosBot/OpenClaw integrations.

## Commands

```bash
npm run -w ./workspace-server start
npm run -w ./workspace-server test:run
```

Canonical product/deployment documentation is in `../docs`.
